[project]
name = "llm-uncertainty-decomp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cyclopts>=4.3.0",
    "probably>=1.1.3",
    "ruff>=0.14.9",
]
